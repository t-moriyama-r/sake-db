# .lefthook.yml
pre-push:
  parallel: true
  commands:
    eslint:
      run: npm run lint --fix
    prettier:
      run: npm run format

post-push:
  commands:
    message:
      run: echo "プッシュが完了しました。"
