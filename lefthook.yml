# lefthook.yml
# This extends both frontend and backend directory's lefthook configurations
# Each directory (front, backend, etc.) has its own .lefthook.yml
extends:
  - front/.lefthook.yml
  - backend/.lefthook.yml
